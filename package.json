{
  "name": "dbee",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter './apps/*' dev",
    "build": "bun run --filter '@dbee/web' build && bun run --filter '@dbee/server' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint . && bun run check:bytes",
    "test": "bun test",
    "check:bytes": "bun run scripts/check-source-bytes.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.4.1",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.12.0",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.69.0"
  },
  "dependencies": {
    "@fontsource-variable/space-grotesk": "^5.3.0"
  }
}
