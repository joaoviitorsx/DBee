/**
 * Schemas TypeBox e tipos compartilhados entre server e web (DBee.md §3).
 *
 * Vazio de propósito no scaffold: os schemas chegam junto com as rotas que os
 * usam. O que este arquivo estabelece agora é só o ponto de export do pacote.
 */

export {};
